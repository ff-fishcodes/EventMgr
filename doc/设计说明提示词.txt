07.06 17:27
根据我的文档章节格式要求，重新生成一版软件设计说明文档，不覆盖原文档，文档章节名称后面的括号内为该章节唯一标识符的格式。模板如下：
1.文档标识
2.引用文档
3.CSCI级设计决策
分条给出构件设计决策，忽略内部实现，从用户角度出发描述构件将怎么运行以满足需求，和其他影响组成该构件的选择与设计的原则。如果设计决策依赖于系统状态或方式，则指明依赖性。
4.CSCI体系结构设计
4.1CSCI部件
描述构成该软件包含的所有模块，并赋予唯一标识符（CSC_xxx），列出软件模块的用途，模块间的关系和结构，满足需求所需的硬件资源等。部件是对需求的回应。
4.1.x （CSC_xxxx）
4.2执行方案
说明软件单元间的执行方案，采用图表和描述，说明软件单元间的动态关系，互相作用情况，包括执行控制流程，数据流，状态转换图，时序图，对象、进程、任务的动态创建、删除。
4.2.x (MS_xxx)
4.3接口设计
4.3.1接口标识和接口图
说明每个接口的唯一标识符，有哪些接口特征，应通过接口图来描述。每个接口的具体描述在4.3.x章节中描述
4.3.x（JK_xxx）
描述接口的具体特性（数据类型，数据范围，通信方法，消息格式，协议特征等）
5.CSCI详细设计
本章分为以下子章节，分别描述每个软件单元的详细设计
5.x xx接口（CSU_xxx）
单元设计决策，设计约束限定，软件逻辑，软件单元执行过程，详细的时序，数据流，类图等
6.需求可追踪性
与需求规格说明书构成追溯关系，本文档所标识的每个单元与需求规格说明的对应关系。也需要每个需求到本文档所标识的每个软件单元的对应关系。

