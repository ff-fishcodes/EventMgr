# 事件管理

本领域接收报警条件、维护当前活跃事件，并协调执行已配置的响应动作。

## 领域语言

**事件**：
由来源名称、帧 ID 和报警字段共同标识的一次运行时状态。事件产生后保持活跃，直到被明确消除。
_避免使用_：报警实例、通知

**报警定义**：
由用户注册的事件定义，包含事件描述、原始等级和来源类型。
_避免使用_：系统事件定义、设备事件定义

**联动动作**：
事件产生或消除时可调用的、具有唯一名称的响应能力。每个联动动作只能有一个注册和一种执行方式。
_避免使用_：回调、处理器、联动项

**事件联动**：
事件在产生阶段或消除阶段与联动动作之间的配置关系。
_避免使用_：动作绑定、事件回调列表

**执行上下文**：
与联动动作关联、由外部维护的 QObject。该动作通过 Qt 队列连接进入此对象所属线程执行；对象生命周期和线程配置由注册方负责。
_避免使用_：目标线程、接收线程

**动作可用性**：
联动动作当前是否具有可执行注册；对于绑定执行上下文的动作，还要求执行上下文仍然存活。可用性不同于用户配置的事件动作开关。
_避免使用_：启用状态、注册状态

**动作启用开关**：
用户针对一个事件阶段配置的开关，用于允许或抑制某个联动动作。动作最终可执行需要开关开启且动作可用。
_避免使用_：可用性、全局动作状态
